# 単語帳共有リンク機能 — 調査・設計ドキュメント

作成日: 2026-07-12
ステータス: 調査完了・設計案。**このラウンドでは実装しない**（次回以降の判断材料）。

---

## 0. 最重要の発見: 共有リンク機能は既に一部実装済み

このドキュメントの作成にあたって既存コードを調査した結果、「単語帳を共有URLにする」機能は
**ゼロから作る新機能ではなく、既に本番稼働中**であることが判明した。まずこの事実を正確に
共有することが、以降の設計判断より重要と考える。

### 既に存在するもの

| 要素 | ファイル | 内容 |
|---|---|---|
| DBカラム | `supabase/migrations/005_wordbook_share.sql` | `word_books.share_code`（unique）・`word_books.is_shared`（boolean） |
| 共有ON/OFF API | `src/app/api/wordbook/[id]/share/route.ts` | `POST`で`share_code`発行+`is_shared=true`、`DELETE`で`is_shared=false`（所有者のみ、認証必須） |
| インポートAPI | `src/app/api/wordbook/[id]/import-shared/route.ts` | 共有単語帳を自分の単語帳としてコピー（**認証必須**、最大5000語） |
| 共有UI | `src/components/wordbooks/ShareButton.tsx` | `/wordbooks/[id]`ページに設置済み。共有ON/OFF・URLコピー |
| 公開ページ | `src/app/share/[code]/page.tsx` + `SharedWordbookImport.tsx` | `share_code`から単語帳タイトル・説明・レベル・語数・**最初の10語のプレビュー（word/meaningのみ）**を表示。未ログインでも閲覧可能 |
| RLS | `supabase/rls.sql` | `word_books`・`words`とも`auth.uid() = user_id`のみ（匿名向けpolicyは無し） |
| robots.txt | `public/robots.txt` | `Disallow: /share/` 済み |
| sitemap | `src/app/sitemap.ts` | `/share/*`は含まれていない（意図的か偶然かは不明だが、結果として除外されている） |

### 現状のアクセス制御の仕組み

`/share/[code]/page.tsx` は Supabase の **anon key ではなく service_role（`createAdminClient()`）** で
`word_books`/`words` を取得している。RLS 側には匿名向け SELECT policy が一切無いため、
「万が一このページの実装を anon client に書き換えてしまっても、データが漏れるのではなく
黙って0件になる」フェイルセーフな構造になっている。これは意図的か偶然かは不明だが、
良い設計になっている。

---

## 1. 目的（今回のオーナー要望との差分）

オーナーが今回のPhase 4で描いていた機能像:

- ユーザーが自分の単語帳を共有URLにできる → **実装済み**
- 共有URLを開いた人はログインなしで**一部テストできる** → **未実装**（現状は受動的なプレビュー表示のみ。フラッシュカードやクイズ形式の対話的な学習はできない）
- もっと使いたい場合は無料登録 → **実装済み**（インポートボタン押下時に未ログインなら`/signup`へ誘導）
- 共有ページは必要に応じてnoindexにできる → **部分実装**（`robots.txt`でDisallow済みだが、ページ自体に`noindex`メタタグは無い）
- 公開範囲をユーザーが制御できる → **実装済み**（ON/OFFのみ。公開範囲の粒度調整＝例えば「プレビューのみ公開」「フル公開」等の段階制御は無い）
- 個人情報やメモが漏れない → **実装済み（結果的に安全）**。詳細は3章
- 市販教材由来データの扱いに注意する → **未対応（要対策）**。詳細は4章

つまり、Phase 4で本当に必要なのは「ゼロから設計する」ことではなく、**既存実装とオーナーの
想定とのギャップを埋める追加開発の設計**である。

---

## 2. ユースケース

1. **学生同士**: 定期テスト前、クラスメイトが作った単語帳のURLをLINE等で送り合う
2. **先生→生徒**: 塾講師・学校教員が授業で使った単語リストをクラス全体に配布する（`/guide/vocabulary-quiz-pdf-for-teachers`のPDF導線と補完関係になる — 紙で配るか、URLで配るかの選択肢）
3. **SNSでの拡散**: X等で「この単語帳作った」と共有し、興味を持った人が試しに使う→登録する、というバイラルループ

---

## 3. データモデル（現状 + 拡張案）

### 現状のスキーマ（変更不要）

```sql
-- 既存: supabase/migrations/005_wordbook_share.sql
word_books.share_code text unique
word_books.is_shared  boolean not null default false
```

### 個人情報・メモの漏洩リスク評価

`words`テーブルには自由記述の「メモ」的なカラムは存在しない
（`etymology`/`nuance`/`similar_words`/`antonym`/`derivative`/`idiom`はいずれも単語の学習補助
情報であり、ユーザーが個人的な感想やメモを書く欄ではない）。`/share/[code]`が選択している
カラムは`word_books`側が`id, title, description, level, exam_type, user_id`、`words`側が
`word, meaning`のみで、`user_id`はサーバーコンポーネント内で取得されるだけでクライアントに
渡すpropsには含まれていないため、HTMLにも出力されない。**現状、個人情報が漏れる実装には
なっていない。**

ただし、`word_books.title`・`description`はユーザーの自由入力であるため、ユーザーが
自分で個人名やメールアドレスをタイトルに書いてしまうケースは技術的に防げない
（バリデーションでは検出不可能な運用リスク）。これは「機能のバグ」ではなく「ユーザー行動の
リスク」として、共有ON時にUI上で一言注意喚起する価値はある（例:「タイトル・説明文に
個人が特定される情報を含めないでください」という一文をShareButton付近に追加）。

### 拡張案（MVP完成に向けて追加が必要なカラム）

```sql
-- 案: 006_wordbook_share_extend.sql（未実装・設計のみ）
alter table public.word_books
  add column if not exists share_source_locked boolean not null default false;
comment on column public.word_books.share_source_locked is
  '市販教材由来の単語を含む単語帳の共有を許可制にするためのフラグ（4章参照）。';
```

---

## 4. 最重要リスク: 市販教材データの共有経由での再配布

### 何が問題か

このサービスは`material_words`（許諾済み公開教材）と、ユーザーが自分で入力した`words`を
明確に区別し、「市販教材データを無断で再配布しない」という方針をこれまでのラウンドで
一貫して守ってきた（`/legal/content-policy`参照）。

しかし現在の共有機能は、**単語の出所（`source_type`）を一切見ずに、どんな単語帳でも
共有可能**になっている。`word_books.source_type`は`custom`/`material`/`system`の3値を
持つが、`api/wordbook/[id]/share/route.ts`はこの値をチェックしていない。

つまり理論上、あるユーザーが許諾済み公開教材（`material`由来）を自分の単語帳にインポート
した後、その単語帳を「共有」してshare_codeを発行し、他のユーザーが`import-shared`で
無制限にコピーする、という流れが技術的に可能になっている。これは意図しない
ユーザー間再配布の抜け道になり得る。

### 推奨対策（MVP時点で入れるべき最小限のガード）

```ts
// api/wordbook/[id]/share/route.ts のPOSTハンドラに追加するチェック案
const { data: book } = await supabase
  .from("word_books")
  .select("id, source_type")
  .eq("id", id).eq("user_id", user.id).maybeSingle();

if (book?.source_type === "material") {
  return NextResponse.json(
    { error: "許諾教材からインポートした単語帳は共有できません。自分で作成した単語帳のみ共有可能です。" },
    { status: 403 }
  );
}
```

`source_type === "custom"`（ユーザーが自分で単語を追加した単語帳）のみ共有を許可し、
`material`（教材インポート由来）は共有不可にするのが最もシンプルで安全な線引き。
`system`（今後追加され得るシステム提供単語帳）も同様に不可とする。

---

## 5. noindex方針

### 現状

- `robots.txt`で`/share/`配下を`Disallow`（クロール自体をブロック）
- ページ自体に`<meta name="robots">`は無い

### 既知の落とし穴

Googleの仕様上、`robots.txt`の`Disallow`は「クロールしない」ことしか保証しない。
外部サイトからそのURLへのリンクがあれば、**クロールせずにURLだけをインデックスする**
ことがある（タイトル無し・スニペット無しの状態で検索結果に出ることがある）。
確実に検索結果から除外したい場合は、`Disallow`ではなく`noindex`メタタグ（かつクロールは
許可する）が本来のGoogle推奨手法だが、この2つは併用すると「noindexタグ自体が読まれない」
という矛盾が起きるため、以下のいずれかの方針に統一する必要がある。

### 推奨方針

すべての共有ページを常に`noindex`とし、切り替え不要とする（ユーザーが「これは
indexしてほしい」と選べるようにする必要性は薄く、複雑さの割にメリットが小さい）。

```ts
// src/app/share/[code]/page.tsx に追加する案
export async function generateMetadata(): Promise<Metadata> {
  return { robots: { index: false, follow: false } };
}
```

これを追加する場合、`robots.txt`の`Disallow: /share/`は**外す**必要がある
（noindexタグをクローラーに読ませるにはクロールを許可しなければならないため）。
つまり「`robots.txt`のDisallowのまま」か「Disallow解除＋noindexタグ追加」かの二択であり、
現状の「Disallowのみ」は不完全ではあるが実害は小さい（大量共有が起きていない現段階では
優先度は低い）。将来、共有機能の利用が増えてから対応すれば十分。

---

## 6. RLS方針（現状維持を推奨）

現状の「word_books/wordsとも所有者のみRLS、共有ページはservice_role経由」という構成は
変更不要と判断する。理由:

- 匿名ユーザー向けのRLS policyを新設する（例: `is_shared = true`なら誰でもSELECT可）方式は、
  Supabaseの匿名APIキーを知っている全クライアントから直接クエリされる経路が増えることを
  意味し、攻撃面が広がる（share_codeの総当たりでの列挙リスクが上がる、選択カラムを
  アプリ側で絞り込めない等）。
- 現状のservice_role経由（サーバーコンポーネントでのみ実行・選択カラムを明示的に絞る）の方が、
  「見せてよいものだけを明示的に選ぶ」設計として安全。

---

## 7. 匿名で一部テストできる機能（MVP拡張案）

オーナーが望んでいる「ログインなしで一部テストできる」を実現する場合の最小実装案:

- `/share/[code]`のプレビュー10語を、現在の受動的なリスト表示から、**フラッシュカード
  形式（表:単語 → タップで裏:意味）**の簡易UIに変える。新規テーブル・API不要、
  既存の10語プレビューをクライアント側でめくれるようにするだけ。
- 4択クイズ形式にはしない（正誤判定・スコア保存が必要になり、匿名ユーザーの結果を
  どう扱うかという新たな設計判断が発生するため、MVPの範囲を超える）。
- 「もっと見る・全語をテストするには登録」という導線は現状の「インポートには登録」
  という導線をそのまま流用できる。

この部分は次回以降の実装候補であり、今回は設計のみに留める。

---

## 8. MVP仕様（次回実装する場合の最小スコープ）

現状の実装に対して、以下3点を追加するだけで「調査・設計」で見つかったギャップの
大部分が埋まる。いずれも小さく、既存機能への破壊的変更を伴わない。

1. `source_type === "material"`の単語帳は共有不可にする（4章、`share/route.ts`に数行追加）
2. `/share/[code]`に`noindex`メタタグを追加し、`robots.txt`のDisallowを外す（5章）
3. ShareButton付近に「タイトル・説明文に個人が特定される情報を含めないでください」の注意書きを追加（3章）

「匿名で一部テストできる」（7章）は上記3点より優先度を落としてよい
（現状のプレビュー表示でも「どんな単語帳か分かる」という最低限の目的は果たせているため）。

---

## 9. 将来拡張

- 共有単語帳のランキング（人気の共有単語帳を`/materials`のように一覧化する）→ Phase 5の
  独自レポート構想と相性が良いが、個人が特定されない集計にする必要がある
- 先生向け機能との統合: 現在の「先生向け機能」はクラス単位の学習状況閲覧のみ。
  共有単語帳を「クラスの教材」として先生が生徒に配布する導線に発展させる余地がある
- 共有単語帳への「いいね」「保存数」などの軽量なソーシャル機能（ただし個人情報を伴わない
  集計値のみに限定する）

---

## 10. 実装リスク

| リスク | 深刻度 | 備考 |
|---|---|---|
| 市販教材由来の単語帳が無制限に共有・複製される | 高 | 4章。著作権・利用規約上のリスク。優先度最高 |
| noindexタグ無し（robots.txtのDisallowのみ） | 中 | 5章。共有数が少ない現段階では実害は限定的 |
| ユーザーがタイトル・説明文に個人情報を書いてしまう | 低 | 3章。技術的に防げないため注意喚起で対応 |
| share_codeの総当たり | 低 | 33文字×10桁 ≈ 1.4×10^15通り。実用上問題ない強度 |
| 共有単語帳の大量共有によるDB負荷 | 低 | 現状レート制限は無いが、共有が主要導線化するまでは優先度低 |

---

## 11. 必要テスト（次回実装時に追加すべきもの）

現状、`/share/[code]`・共有ON/OFF・インポートフローを検証するE2Eテストは**存在しない**
（`test:diagnostic-share`は`/vocab-check`のXシェアのみを対象としており無関係）。
次回実装時は以下を検証する`test:wordbook-share`を新設することを推奨する。

- 単語帳詳細ページで「共有」ボタンを押すとshare_codeが発行され、URLが表示される
- 発行されたURLに未ログインでアクセスすると200で表示され、プレビュー（10語まで）が見える
- 未ログインで「インポート」を押すと`/signup`にリダイレクトされる（DBには書き込まれない）
- ログイン済みで「インポート」を押すと自分の単語帳としてコピーされ、`/wordbooks/[id]`に遷移する
- 「共有を停止」を押すと`is_shared=false`になり、共有URLへのアクセスが404になる
- `source_type === "material"`の単語帳では共有ボタンが表示されない、またはエラーになる（4章の対策を実装した場合）
- 共有ページに`noindex`メタタグが出力される（5章の対策を実装した場合）
- 個人情報（メールアドレス等）がHTMLに含まれない
