# SEARCH_CONSOLE_SETUP — Google Search Console 登録ガイド

> Search Console の画面操作はオーナー側で行う。本書はその手順と、登録後の運用方法をまとめたもの。

## 0. 現在のステータス（2026-07-01 登録完了、2026-07-04 sitemap更新）

- **プロパティ**: `https://loop-vocabulary.app`（URLプレフィックス）— 登録・所有権確認 済み
- **sitemap送信**: `/sitemap.xml` を送信済み。ステータス **Success（成功）**、**Discovered pages: 69**、Discovered videos: 0（送信日 2026-07-01）
- **2026-07-04追記**: カテゴリ別公開LP `/materials/toeic`・`/materials/business`を新設し、
  sitemap.xmlに追加した（詳細は§0-1参照）。robots.txtの変更は無し（既存の許可設定のままで
  ブロックされていないことを確認済み）
- **次のアクション**: 数日〜1週間ほど待ってから「ページ」タブでインデックス状況・警告を確認する（§4「インデックス未登録時の対応」参照）。新規追加した2LPは、URL検査ツールで個別に
  インデックス登録をリクエストすると早く反映される可能性がある（§0-1参照）

### 0-1. 次にSearch Consoleで確認・登録すべきURL（2026-07-04追加分）

以下のURLをURL検査ツールで個別に検査し、「インデックス登録をリクエスト」を実行することを
推奨する（sitemap再クロールを待たずに反映を早められる）。

- `https://loop-vocabulary.app/materials/toeic`（新規）
- `https://loop-vocabulary.app/materials/business`（新規）
- `https://loop-vocabulary.app/materials`（既存、再クロール推奨。カテゴリLPへのリンクを追加したため）
- `https://loop-vocabulary.app/dictionary`（既存、再クロール推奨。教材一覧へのリンクを追加したため）

> 補足: 本サイトのsitemapには78件のURLが含まれるが、GSC側の「検出されたページ数」は69件。
> 送信直後は未クロール分がこの数に反映されるため、通常はクロールの遅延によるもので問題ない。
> 1週間経っても69件付近で頭打ちの場合のみ、どのURLが未検出か「ページ」タブで確認する。

---

## 1. 登録前チェック結果（2026-07-01 実施・修正済み）

| 項目 | 結果 |
|---|---|
| `https://loop-vocabulary.app/sitemap.xml` が本番で正常取得できるか | ✅ 200・78件のURLを含む |
| sitemapに主要ページが入っているか | ✅ トップ・辞書・教材一覧/詳細・ガイド記事・文法レッスン・FAQ・語彙力チェック等 |
| robots.txt があるか | ✅ `https://loop-vocabulary.app/robots.txt` に存在、`Sitemap:`行あり |
| noindexが必要なページだけに付いているか | ✅ `/login`はnoindex設定済み。sitemapからも除外（登録前チェックで発見・修正） |
| 公開ページが200で返るか | ✅ `npm run verify:prod`で確認済み |
| 認証ページがindex対象になっていないか | ⚠️→✅ **修正済み**。以下の不整合を発見し本番修正・反映済み |

### 発見・修正した不整合

1. **sitemapに認証必須ページが混入していた**: `/login`（noindex設定と矛盾）・`/test`・`/test/attack`・`/plan`・`/extract`（いずれも未ログインで`/login`に307リダイレクトされる）が sitemap に含まれていた → 全て除外
2. **`/dictionary`が sitemap に無かった**: Phase 1で登録不要化した公開ページだが未収録だった → 追加
3. **robots.txt が新しいルートに未対応だった**: `/teacher` `/pdf` `/join/` `/plan` `/extract` `/learn` `/ai` `/ranking` `/road` `/share/` `/setup` の Disallow が漏れていた → 追加。また `Disallow: /test/`（末尾スラッシュ付き）では `/test` 自体がブロックされない不備があったため `Disallow: /test` に修正（`/wordbooks` `/admin` も同様に修正）

修正は本番デプロイ・`tsc`/`build`/`test:smoke`/`test:e2e`/`verify:prod` で検証済み（コミット `d4b0db5`）。

---

## 2. Search Console 登録手順（オーナー操作）

### 2-1. プロパティタイプの選択

**推奨: URLプレフィックスプロパティ**（`https://loop-vocabulary.app`）

- **ドメインプロパティ**はDNSのTXTレコード確認が必須で、`loop-vocabulary.app`ドメインのDNS管理画面へのアクセスが必要（Vercelのドメイン設定とは別に、ドメインレジストラ側の操作が要る場合がある）
- **URLプレフィックスプロパティ**は以下のいずれかで確認でき、Vercel環境でも簡単:
  - **HTMLファイルアップロード**: `public/`配下に確認用ファイルを置いてデプロイ（1コミットで完了、私が対応可能）
  - **HTMLタグ**: `<meta name="google-site-verification" ...>` を `layout.tsx` に追加（私が対応可能）
  - **Google Analyticsで確認**: 既に`NEXT_PUBLIC_GA_ID`が設定済みなら、GAとSearch Consoleが同じGoogleアカウントに紐づいていれば自動確認できる場合がある（最も手間が少ない）

**Vercelドメインの場合の推奨**: URLプレフィックス＋HTMLタグ確認が最もシンプル。DNS操作が不要で、確認用メタタグの追加も私が数分で対応できる。

### 2-2. 登録手順

1. <https://search.google.com/search-console> にアクセスし、Googleアカウントでログイン
2. 「プロパティを追加」→ **URLプレフィックス**を選択 → `https://loop-vocabulary.app` を入力
3. 確認方法を選択:
   - **既にGAが設定済みなら「Google アナリティクス」を選ぶと即確認できることが多い**（推奨・最速）
   - それ以外なら「HTMLタグ」を選び、表示されたメタタグを私に共有してください。`layout.tsx`に追加してデプロイします
4. 確認が完了すると「所有権を確認しました」と表示される

### 2-3. sitemap送信手順

1. Search Console左メニュー「サイトマップ」を開く
2. 「新しいサイトマップの追加」に `sitemap.xml` と入力（フルURLではなくパスのみ）
3. 「送信」をクリック
4. ステータスが「成功しました」になるまで数分〜数時間待つ（初回は反映に時間がかかることがある）

### 2-4. 登録後に見るべき項目（初回）

- **「サイトマップ」**: 検出されたURL数が実際のsitemap件数（現在78件）と近いか
- **「ページ」（旧カバレッジ）**: 「インデックス登録済み」「除外」の内訳。除外理由に想定外のものが無いか
  - 想定される除外: 「noindexタグにより除外」（`/login`など、robots.txtでブロック済みなら「robots.txtによりブロック」）
  - 想定外の除外があれば要調査（例: 公開ページなのに「重複していると Google が判断」等）
- **「URL検査ツール」**: トップページ・`/dictionary`・`/materials`など主要ページを個別に検査し、「インデックス登録可能」と出るか確認

### 2-5. まず確認すべきエラー

- **「送信されたURLにnoindexタグが追加されています」**: sitemapに入れたページに誤ってnoindexが付いていないか（今回のようなミスマッチ）
- **「サーバーエラー(5xx)」**: クロール時に本番が落ちていた形跡。Vercelログと突き合わせる
- **「リダイレクトエラー」**: 想定外のリダイレクトループが無いか
- **「見つかりませんでした(404)」**: sitemapに古い/削除済みURLが残っていないか
- **「クロール済み - インデックス未登録」**: 内容が薄い、または重複していると判断された可能性。ガイド記事等で頻発する場合は内容の見直しを検討

---

## 3. 毎週見る項目

- **検索パフォーマンス**タブ:
  - **合計クリック数・合計表示回数**の週次トレンド（急増・急減がないか）
  - **平均CTR（クリック率）**: 表示されているのにクリックされていないページがないか → タイトル/メタディスクリプションの見直し候補
  - **平均掲載順位**: 主要キーワード（英単語 アプリ、英検 単語帳 等）の順位推移
  - **クエリ**タブで、どんな検索語句から流入しているか（想定外のクエリで大量流入していれば、そのニーズに合わせたコンテンツ追加を検討）
  - **ページ**タブで、どのページが最もクリックされているか（伸びているガイド記事があれば関連記事を増やす判断材料に）
- **ページ（カバレッジ）**タブ: インデックス数の増減、新規エラーの有無
- **サイトマップ**タブ: 送信済みURL数とインデックス済み数の乖離が大きくなっていないか

## 4. インデックス未登録時の対応

1. **URL検査ツール**で対象URLを検査し、「インデックス登録をリクエスト」を試す（即時ではないが優先度が上がる）
2. 除外理由を確認:
   - 「重複しています」→ 内容が他ページと酷似していないか確認（教材ページ等で語数が極端に少ない場合など）
   - 「クロール済み-インデックス未登録」→ コンテンツの質・独自性を見直す。内部リンク（他ページからのリンク）が少ない場合は追加を検討
   - 「検出されましたが未登録」→ クロール待ちの可能性が高い。数週間様子を見る
3. sitemapの`lastModified`が実態を反映しているか確認（本アプリの現状は全ページ`new Date()`固定のため、更新頻度のシグナルとしては弱い。将来的にDB上の実更新日時を使う改善余地あり — [NEXT_IMPROVEMENTS.md](NEXT_IMPROVEMENTS.md)参照）

## 5. 検索クエリ・掲載順位・クリック率の見方

- **検索パフォーマンスレポート**の各指標:
  - **表示回数**: そのクエリでサイトが検索結果に出た回数（クリックされなくてもカウント）
  - **クリック数**: 実際にクリックされた回数
  - **CTR**: クリック数 ÷ 表示回数。同じ順位でもタイトルが魅力的でないとCTRは低くなる
  - **掲載順位**: 平均順位（1に近いほど上位）
- **見るべき組み合わせ**:
  - 表示回数が多いのにCTRが低い → タイトル・メタディスクリプションの改善余地（クリックされていない = 検索結果で選ばれていない）
  - 掲載順位が2ページ目以降（11位以下）で表示回数も少ない → コンテンツの質・被リンク不足。無理に追わず、他の伸びているクエリに注力する方が効率的
  - 特定のガイド記事だけ表示回数が急増 → そのテーマの関連記事を増やす好機（[NEXT_IMPROVEMENTS.md](NEXT_IMPROVEMENTS.md)の「SEO記事の追加」参照）

---

## 登録完了後の役割分担

**私（Claude）が対応できること**
- sitemap/robots.txtの継続的な整合性チェック（新ルート追加時など）
- 「ページ」タブの除外理由・エラー文言を共有いただければ調査・修正対応
- 検索パフォーマンスのデータ（クエリ・CTR・順位）を共有いただければ傾向分析・改善提案

**オーナーが継続して行う操作**
- 週次でのSearch Console画面の閲覧（本書§3の項目）
- 「ページ」タブでの除外理由・警告の確認（本書§4）
- 気になる結果があれば、文言やスクリーンショットを共有

## 次回チェック時に共有いただきたい情報（1週間後目安）

- 「ページ」タブのインデックス済み/除外の内訳（Discovered pagesの69件からどう変化したか）
- 除外理由に想定外のものがないか
- 検索パフォーマンスで表示回数・クリックが出始めているか
- **2026-07-04追加**: `/materials/toeic`・`/materials/business`のインデックス登録状況
  （§0-1のURL検査ツールでリクエスト後、実際に登録されたか）。TOEIC・ビジネス英語関連の
  検索クエリで表示が出始めているか
