# 無料ツールSEO実装ロードマップ（Phase 7）

## 今回実装したもの

`/tools` ハブページ（`src/app/tools/page.tsx`）を新規実装し、sitemap.tsに追加した。

- 既存の生きている機能への導線: 語彙力診断(`/vocab-check`)・辞書検索(`/dictionary`)・教材/単語帳インポート(`/materials`)・小テストPDF作成(`/guide/vocabulary-quiz-pdf-for-teachers`経由)
- 未実装の2ツールは「準備中」カードとして明示し、まだ存在しないURLへのリンクは張っていない（存在しないページへの内部リンクによるクロールエラーを避けるため）

## 検討した4候補と、今回の扱い

| 候補 | 検索意図 | 今回の扱い |
|---|---|---|
| 英単語レベル診断 | 「英単語 レベル 診断」等 | **既存の`/vocab-check`が同じ意図をすでに満たしている**ため、新規ページは作らず`/tools`から直接導線を張った（重複コンテンツ回避） |
| 英単語小テスト/PDF作成ツール | 「英単語 小テスト 作成」等 | **既存の`/pdf`機能 + `/guide/vocabulary-quiz-pdf-for-teachers`記事がすでに対応**。`/tools`から導線を張った |
| 試験日から逆算する学習計画 | 「英検 試験日 逆算 学習計画」等 | 未実装。`/tools`に準備中として明示のみ |
| 英単語リスト整形・CSV変換 | 「英単語 リスト CSV 変換」等 | 未実装。`/tools`に準備中として明示のみ |

## 次回実装するなら

### `/tools/exam-countdown-study-plan`（試験日から逆算する学習計画）
- 入力: 試験種別（英検級/TOEIC）・試験日・現在の語彙力チェック結果（任意）
- 出力: 今日から試験日までの日数、1日あたりの推奨新規学習語数、推奨教材へのリンク
- 実装コスト: 中（既存の`get_material_word_counts` RPCと日付計算のみ、AIコスト不要）
- SEO価値: 高（「英検 いつから 勉強」「TOEIC 1ヶ月前 単語」等の検索意図に直接応える）

### `/tools/word-list-cleaner`（英単語リスト整形・CSV変換）
- 入力: テキストエリアに単語リストを貼り付け（改行区切り・カンマ区切り等）
- 出力: 単語帳インポート用CSV形式にクライアントサイドで整形（サーバーに保存しない・DBに書き込まない設計にすることで、外部データの無断保存リスクを回避できる）
- 実装コスト: 中（クライアントサイドのテキスト処理のみ）
- 注意: ユーザーが貼り付けた単語リストが市販教材の丸写しである可能性を完全には排除できないため、「ご自身で作成した単語リストのみをご利用ください」という注意書きが必須（`CONTENT_SOURCE_POLICY.md`参照）

両ツールとも、実装する場合は本ラウンドで新設した `test:tools-hub` と同様のE2Eテストを追加すること。
