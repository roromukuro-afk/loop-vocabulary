# Loop Vocabulary — 実操作テストチェックリスト

本番デプロイ前に、手動で確認するチェックリスト。
チェック欄を埋めながら一通り回せば「本番で致命傷が出ない」状態になる想定。

## 0. 起動前提

- [ ] `.env.local` に `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` が設定済み
- [ ] Supabase Studio で `schema.sql` → `rls.sql` → `seed.sql` を実行済み
- [ ] `npm run dev` で開発サーバが起動
- [ ] ブラウザで `http://localhost:3000` (使用中なら 3001 等) が開ける

## 1. Supabase 未設定時の動作

`.env.local` を一時的にリネームして起動。

- [ ] `/dashboard` にアクセスすると `/setup` にリダイレクトされる
- [ ] `/setup` に「Supabase 未設定」のメッセージと設定手順が表示される
- [ ] LP `/`, `/privacy`, `/terms`, `/premium` は引き続き表示される
- [ ] `/login`, `/signup` も表示される（フォーム送信時に `/setup` に飛ぶ）

## 2. ユーザー認証

- [ ] `/signup` で新規メール+パスワード登録ができる
- [ ] (確認メールがある場合) メール内リンクで `/dashboard` に飛ぶ
- [ ] `/login` で既存アカウントにログインできる
- [ ] ログイン後、`/dashboard` に遷移する
- [ ] `/settings` → 「ログアウト」で `/login` に戻る
- [ ] ログアウト中に `/dashboard` を直接開くと `/login` にリダイレクト

## 3. 単語帳

- [ ] `/wordbooks` で「新しい単語帳を作る」フォームから作成できる
- [ ] レベル選択（中学基礎〜参考書別）ができる
- [ ] 作成した単語帳が一覧に並ぶ
- [ ] 単語帳をタップして詳細 `/wordbooks/[id]` に入れる
- [ ] 「＋ 単語追加」から `/wordbooks/[id]/add` に遷移、すべての項目が入力できる
- [ ] 単語登録後、単語帳詳細に表示される

## 4. 辞書検索

- [ ] `/dictionary` で `increase` 等を検索して結果が出る (seed済み教材から拾える)
- [ ] 検索結果カードで「教材」/「自分の単語」のラベルが正しい
- [ ] 「＋ 単語帳に追加」で自分の単語帳に追加される
- [ ] 追加先の単語帳をプルダウンから変更できる

## 5. 爆速 4 択テスト

- [ ] `/test/choice` (単語が 4 以上必要) でテスト開始できる
- [ ] 1問ずつ表示され、選択肢4個から1つを選べる
- [ ] 正解は緑、誤答は赤、正解は強調される
- [ ] 「次へ」でテンポよく進める
- [ ] 10問終わったら結果画面 (正答率/間違えた単語) が出る
- [ ] テスト中に広告 (Banner/Native) が表示されない
- [ ] 結果が `study_results` テーブルに保存される（Supabase Studio で確認）
- [ ] `words.streak` / `words.next_review_at` が SRS ルールで更新される

## 6. 入力テスト

- [ ] `/test/input` で意味から英単語を入力するテストができる
- [ ] 完全一致で正解になる
- [ ] 大文字小文字違い・前後空白は正解扱い
- [ ] 1〜2文字違い（例: `provid` → `provide`）で「惜しい」表示が出る
- [ ] 全然違う回答は「不正解」で正解が表示される
- [ ] 結果が SRS / 学習履歴に反映される

## 7. 復習 (忘却曲線)

- [ ] `/review` に `next_review_at <= now` または苦手フラグありの単語が並ぶ
- [ ] 単語が4つ以上あれば「復習を始める」が押せる
- [ ] 復習プレイ中は広告非表示
- [ ] 正解で次回復習日が伸びる (1→3→7→14→30 日)
- [ ] 不正解で `is_weak=true` になる
- [ ] 翌日に該当単語が `/review` に再登場する (日付固定で SQL を直接確認)

## 8. 苦手単語

- [ ] `/weak` で苦手単語が並ぶ
- [ ] 5つの並び替え (不正解多い / 正答率低い / 古い / 復習近い / 重要度) が動く
- [ ] 苦手フラグ付きの単語にバッジが表示される

## 9. 学習記録

- [ ] `/stats` で「今日学習」「正答率」「連続学習」が正しく出る
- [ ] 30 日カレンダーの濃さが学習量に応じて変わる
- [ ] テスト終了直後にカウントが増えている

## 10. 教材

- [ ] `/materials` に `is_public=true` & `license_status='approved'` の教材だけ並ぶ
- [ ] 絞り込みチップ (高校入試/英検3級 等) で `?level=` / `?exam=` に切り替わる
- [ ] 教材詳細 `/materials/[id]` で収録単語が表示される
- [ ] 「自分の単語帳にインポート」で word_books / words が一括作成される

## 11. AI 例文・解説 (モック)

- [ ] `/ai` で単語と意味を入れて「生成する」を押すと結果が表示される
- [ ] 4 種類 (例文/解説/語源/覚え方) を切り替えられる
- [ ] 1 日 5 回まで使える
- [ ] 6 回目で「本日の利用上限に達しました」エラーが出る
- [ ] 「リワード広告を見て + 1 回」ボタンを押すとチケットが付与され、もう一度生成できる (`reward_tickets` テーブルに `kind='ai', amount=1` が増える)

## 12. PDF 小テスト

- [ ] `/pdf` で単語帳・教材を選択し、出題数・方向・形式を選べる
- [ ] 「印刷用ウィンドウを開く」で別タブが開き、A4 レイアウトで小テストが表示される
- [ ] 解答欄つきの場合、答えが下線になっている
- [ ] 「解答つき」にチェックを入れると下部に解答リストが出る
- [ ] ブラウザの「PDF で保存」で PDF 化できる
- [ ] `pdf_exports` テーブルにログが残る

## 13. 広告コンポーネント

- [ ] `/dashboard`, `/wordbooks`, `/wordbooks/[id]`, `/weak`, `/review`, `/stats`, `/settings`, `/materials/[id]` に広告枠が見える
- [ ] テスト/復習プレイ中 (`/test/*`, `/review?start=1`) は広告枠が **無い**
- [ ] 全広告に「広告 / Ad」ラベルが付いている
- [ ] `.env.local` で `NEXT_PUBLIC_ADS_ENABLED=false` にすると全広告が消える

## 14. 管理画面

事前に Supabase で `update public.profiles set is_admin = true where email = '<あなたのメール>';` 実行。

- [ ] 管理者で `/admin` にアクセスでき、教材管理・インポートのリンクが見える
- [ ] `/admin/materials` で「＋ 新規追加」フォームが動く
- [ ] 教材の許諾ステータス (pending/approved/denied) を変更できる
- [ ] 公開トグルが効く (非公開にした教材は `/materials` から消える)
- [ ] 教材を削除すると関連 `material_words` も消える
- [ ] `/admin/import` で CSV と JSON 両方を投入できる
- [ ] 投入後、`/materials/[id]` の単語リストに反映される

## 15. 権限分離

- [ ] **一般ユーザーで** `/admin` にアクセスすると `/dashboard` にリダイレクトされる
- [ ] 別ユーザーの単語帳 ID を直接 URL に入れても 404 になる (RLS が効いている)
- [ ] Supabase Studio で他人の `words` を anon key で `select` しても空になる

## 16. PWA

- [ ] Chrome DevTools → Application → Manifest で `Loop Vocabulary` が正しく読まれる
- [ ] アイコン (192/512) が表示される
- [ ] スマホ Chrome で「ホーム画面に追加」できる
- [ ] 追加後、起動するとアドレスバーなしのスタンドアロン表示
- [ ] `favicon.ico` がブラウザタブに表示される
- [ ] `apple-touch-icon.png` が iOS Safari で正しく読まれる

## 17. 表示 (スマホ最優先)

- [ ] iPhone (375px) で全ページが横スクロール無しに収まる
- [ ] ボトムナビが指の届く範囲にある
- [ ] ボタンが押しやすいサイズ (最低 44px)
- [ ] テスト/復習画面が片手操作で完結する
