# 外部由来教材・権利まわり監査（Phase 3: AdSense事前審査 権利/著作権リスク）

作成日: 2026-07-12
対象: `materials` テーブルのうち `is_public = true` かつ `publisher`/`author` が設定されている全教材、
`/share/[code]` 単語帳共有機能、CSV/PDFエクスポート、市販単語帳比較記事（ガイド）。
プロジェクト: Supabase `befjjebsrnsfwhtmydiv`（loop-vocabulary、唯一のプロジェクト。dev/prod共用）。

---

## 1. 対象教材の棚卸し

`is_public = true` かつ `publisher`/`author` が非NULLの教材は44件。そのうち **40件は
`publisher = "Loop Vocabulary"`（自社コンテンツの自己表示）** であり、外部の第三者による
教材は以下の **4件のみ**。

| ID | タイトル | publisher | author | license_status | 語数 |
|---|---|---|---|---|---|
| `00000000-0000-0000-0000-000000000020` | 大学入試頻出英単語 2000+ | 受かる英語 | ukaru-eigo.com | approved | 2,000 |
| `00000000-0000-0000-0000-000000000021` | 中学校英単語 基礎・標準 | 小テストジェネレーター | printgenerator.net | approved | 117 |
| `00000000-0000-0000-0000-000000000022` | 英検2級 重要単語 | VOCABULARISM | vocabularism.com | approved | 799 |
| `00000000-0000-0000-0000-000000000023` | 英検準1級 重要単語 | VOCABULARISM | vocabularism.com | approved | 638 |

4件とも `license_status = "approved"` かつ `license_id` で `licenses`テーブル
（3件: 受かる英語／小テストジェネレーター／VOCABULARISM。022と023は同じ
VOCABULARISMライセンス行を共有）と紐づいている。`licenses`テーブルの中身を
確認したところ:

- `note`列に「サイト運営者から使用許諾取得済み。」という記載はあるが、
  **許諾を得た具体的な日付・連絡先・許諾範囲（例文転載可否等）の詳細を欠く**。
- `approved_by`（社内の承認者）は3件とも`null`。
- `approved_at`は3件とも**完全に同一のタイムスタンプ**（2026-06-27 04:36:40.68）
  であり、実際の許諾取得日ではなく、教材データを一括投入した際のシード時刻を
  そのまま入れたものである可能性が高い。

**この監査ではこれ以上、実際の許諾契約の存在自体を検証することはできない**
（メールや契約書などの一次証跡はコード・DB外にあるため）。よって本監査は
「許諾があるという記録を前提に、(a) その記録の質が十分か、(b) 公開時の見せ方が
安全かどうか」の2点に絞って検証した。(a)については上記の通り記録が簡素すぎる
という指摘を`CONTENT_SOURCE_POLICY.md`にまとめた。

---

## 2. 教材詳細ページの出典表示（検証済み・問題なし）

`src/app/materials/[id]/page.tsx` L219-223:

```tsx
{(material.publisher || material.author) && (
  <div className="text-xs text-navy-500 mt-0.5">
    {[material.publisher, material.author].filter(Boolean).join(" / ")}
  </div>
)}
```

上記4件すべてで `/materials/{id}` にアクセスし、publisher文字列（「受かる英語」
「小テストジェネレーター」「VOCABULARISM」）がページ本文に実際に表示されることを
E2Eで確認済み（`scripts/testing/e2e/external-material-rights.mjs`）。**出典は隠されておらず、
ユーザーから見える形で表示されている。**

---

## 3. 単語帳共有機能 `/share/[code]`（重大な発見あり）

### 3-1. 発見: 想定されていたリスクは修正したが、その前に機能自体が本番で動作していない

`SHARED_WORDBOOKS_DESIGN.md` は「共有機能は既に本番稼働中で、`source_type`を
チェックしていないため市販教材由来の単語帳も無制限に共有できてしまう」と記録していた。
この記述を前提にコード修正を行ったが、**本番Supabase (`befjjebsrnsfwhtmydiv`) の
実際のスキーマを直接クエリで確認したところ、`word_books.share_code` /
`word_books.is_shared` カラムが存在しないことが判明した**：

```
ERROR: 42703: column "share_code" does not exist
```

`supabase/migrations/005_wordbook_share.sql` というファイル自体はリポジトリに存在するが、
`list_migrations` で確認できる本番適用済みマイグレーション一覧には含まれていない
（=**このマイグレーションは一度も本番に適用されていない**）。

結果として:

- `POST /api/wordbook/[id]/share`（共有ON）は、`share_code`/`is_shared`列を含む
  `SELECT`が失敗し、`book`が取得できず常に404（Not Found）を返す。**誰も共有をONに
  できない状態**。
- `GET /share/[code]`（公開プレビューページ）も同様に `share_code` 列でのフィルタが
  失敗するため、**どんなコードを渡しても常に404**。

つまり、設計ドキュメントが前提としていた「市販教材由来の単語帳が無制限に共有できて
しまう」という具体的なリスクは、**現時点では技術的に発生し得ない**（機能自体が
非機能のため）。ただし、これは「安全な設計だから」ではなく「たまたま機能が壊れている
から」であり、意図した安全性ではない。次回このマイグレーションが適用された瞬間に、
ガードなしでは本物のリスクが顕在化する。

### 3-2. 実施した対策（コードは修正済み・マイグレーション適用は対象外）

将来 `005_wordbook_share.sql` が適用されて機能がライブになった際に、ガード漏れで
そのままリスクが顕在化しないよう、**このラウンドでコード側の防御を先回りで実装した**：

1. `src/app/api/wordbook/[id]/share/route.ts`: POSTハンドラで `word_books.source_type`
   を取得し、`"custom"` 以外（= `"material"` や将来の `"system"`）であれば
   `403 Forbidden` を返して共有発行を拒否するガードを追加。
2. `src/app/wordbooks/[id]/page.tsx`: `source_type === "custom"` の単語帳にのみ
   `ShareButton` を表示し、教材インポート由来の単語帳では「教材からインポートした
   単語帳は共有できません」という案内文に差し替え。

`source_type`/`source_material_id` カラム自体は本番DBに実在することを直接クエリで
確認済み（`source_type`: `custom`がデフォルト、現在 `material`が4件・`custom`が7件）。
このガードは今すぐ効く（`source_type`列は使える）が、機能全体は上記3-1の理由で
現状動いていない。

### 3-3. 未実施（このラウンドの対象外・次回への申し送り）

- `005_wordbook_share.sql` マイグレーションの本番適用そのもの。これは「共有機能を
  今回のラウンドでライブにするか」という製品判断であり、`SHARED_WORDBOOKS_DESIGN.md`
  が「このラウンドでは実装しない」と明記していた対象そのものであるため、権利監査の
  範囲を超えると判断し、意図的に適用しなかった。
- マイグレーション適用後に必要な追加対応（`SHARED_WORDBOOKS_DESIGN.md` 5章の
  `noindex`メタタグ、11章のE2Eテスト `test:wordbook-share` 新設）。

---

## 4. 市販単語帳比較ガイド記事（1件修正あり）

対象: `system-eitango` / `target-1900` / `systan-vs-target-1900` / `leap-eitango` /
`eitango-cho-hikaku`（`src/app/guide/[slug]/page.tsx` の `BRAND_REVIEW_SLUGS`）。

### 4-1. (a) 公式提携を主張していないか → 問題なし

いずれの記事にも「Loop Vocabulary は各出版社と公式に提携するものではなく、本記事は
独自の解説・比較です」という注記が既に実装されていた（L2002-2009、
`/legal/content-policy` へのリンク付き）。5記事すべてでE2E確認済み。

### 4-2. (c) 非公式である旨のディスクレーマー → 実装済み（既存）

上記と同一の注記がこれに該当。追加の注記は不要と判断した（既存の文言で
タスクが求める「非公式の比較・学習法解説であり、各教材の発行元とは関係ありません」
という趣旨は十分にカバーされている）。

### 4-3. (b) 教材の実際の単語リスト・並び順の逐語的再現 → **問題を発見し修正**

`systan-vs-target-1900` と `leap-eitango` の2記事に、以下のような見出し・文言で
特定の単語リストを掲載する箇所があった:

- `systan-vs-target-1900`: 「【実データ】最頻出の単語はほぼ同じ」「実際に両者の
  最初の40語を見比べると」という見出しで、両教材の「最初の40語」「Final Stage」
  「Part 3」の実際の収録語を再現したと主張する表を掲載。
- `leap-eitango`: 「【実データ】LEAP最大の特徴は「テーマ別」配列」「実際にLEAPの
  冒頭を見ると」という見出しで、「LEAPの収録語（実際の並び）」とラベル付けした表を
  掲載。

これは以下の理由で問題があると判断した:

1. 単語帳の「見出し語の選定・配列順」自体は編集著作物として保護され得る要素であり、
   それを「実データ」「実際の並び」と明示的に主張して再現するのは、一般的な
   比較コメンタリーの範囲を超え、著作権法上のリスクが単なる感想・比較評より高い。
2. 実際に最新版の当該教材を参照して検証したものではないため（このセッションでは
   物理的な書籍にアクセスできない）、「実データ」という主張自体の正確性も
   検証不能であり、不正確な事実主張のリスクもある。

**対応**: 単語リスト自体（語彙の例示）は一般的な受験頻出語・比較の参考情報として
有用なため削除せず残したが、以下の書き換えを行った:
- 見出しから「【実データ】」を削除。
- 「実際に...を見比べると」「実際の並び」「実際にLEAPの冒頭を見ると」等、
  実際の教材から検証済みの抜粋であるとする主張を、「一般的な傾向」「イメージ」
  「特定の版の収録順を示すものではない」という趣旨の文言に置き換え。
- 「正確な収録語・並び順は各単語帳の最新版でご確認ください」という一文を追記。

`eitango-cho-hikaku` は語数・配列方式などの一般的な属性比較（表）のみで、
「実データ」的な単語リストの主張は含まれておらず、修正不要と判断した。

修正後、E2Eテストで5記事すべてに「実データ」「実際の並び」という文言が
残っていないことを確認済み。

---

## 5. CSV/PDFエクスポートの出典表示（欠落を発見・修正）

### 5-1. PDF（小テスト作成、`src/app/pdf/PdfTestBuilder.tsx`）

修正前は、教材を直接選んで小テストPDFを生成した場合も、教材インポート由来の
単語帳（`word_books.source_type = "material"`）から生成した場合も、出力される
PDFには単語帳/教材のタイトルのみが表示され、**publisher/authorの出典情報は
一切含まれていなかった**（サイレントに欠落）。

**対応**: `src/app/pdf/page.tsx` で教材の `publisher`/`author` と単語帳の
`source_type`/`source_material_id` を取得するよう変更し、`PdfTestBuilder.tsx` の
PDF生成ロジックで、教材直接選択時・教材インポート由来の単語帳選択時のいずれでも
出題用紙・解答用紙の両方に `出典: {publisher} / {author}` の一行を追加した。

### 5-2. CSV（学習データエクスポート、`src/app/api/export/stats/route.ts`）

修正前は、ユーザーの全単語（複数の単語帳を横断してフラット化）をCSV出力する際、
`word_book_id`すら取得しておらず、教材由来かどうかを一切区別していなかった。

**対応**: `words`クエリに `word_book_id` を追加し、該当する `word_books` を
`source_type = "material"` で絞り込んで `source_material_id` を取得、対応する
`materials.publisher`/`author` を解決して、CSVに `source` 列を追加。教材由来の
単語行には出典が、それ以外（自作単語）は空欄が入るようにした。

### 5-3. CSVインポート（`src/app/api/wordbook/[id]/csv-import/route.ts`）

これはユーザーが**自分の**単語をCSVでアップロードして単語帳に追加する機能であり、
教材データの再配布・出典表示とは無関係（インポート＝入力であり、外部への
出力・共有ではない）。監査対象外と判断した。

---

## 6. まとめ: 発見事項と対応状況

| # | 発見事項 | 深刻度（発見当時） | 対応 |
|---|---|---|---|
| 1 | 教材詳細ページの出典表示 | - | 問題なし（既存実装で表示済みを確認） |
| 2 | `/share/[code]`: `source_type`未チェックで市販教材由来単語帳も共有可能 | 高（設計上） | コードにガード追加。ただし実際には基盤マイグレーション未適用のため現状は機能自体が非稼働（3章） |
| 3 | 比較ガイド記事: 公式提携の誤認・非公式ディスクレーマー | - | 問題なし（既存実装で対応済みを確認） |
| 4 | 比較ガイド記事: 「実データ」を称する教材の実際の単語リスト・並び順の主張 | 中 | 2記事（systan-vs-target-1900・leap-eitango）の文言を修正 |
| 5 | PDF小テスト出力に出典（publisher/author）が含まれない | 中 | `出典:`行を出題用紙・解答用紙に追加 |
| 6 | CSV学習データエクスポートに出典が含まれない | 低〜中（現状は該当データなし） | `source`列を追加 |

## 7. 現状のリスクレベル（総括）

- 実際に外部教材（020/021/022/023）からインポートされた単語帳は、監査時点でDBに
  **1件も存在しない**（`source_type='material'`の既存4件はすべて自社教材
  `英検5・4級 基礎単語`由来）。つまり、上記2・5・6の問題は「起きたら危険だった
  潜在的なギャップ」であり、実害の発生を示す証跡は現時点で確認されていない。
- とはいえ、今後ユーザーがこれら4教材をインポート・共有・PDF/CSV出力する可能性は
  ある機能である以上、事前の対策として上記の修正は妥当と判断した。

## 8. 次回以降への申し送り

- `005_wordbook_share.sql` を本番適用する際は、適用前に本ドキュメント3章を必ず
  再確認し、適用と同時に `noindex`メタタグ・`test:wordbook-share`E2Eテストを
  セットで導入すること（`SHARED_WORDBOOKS_DESIGN.md` 参照）。
- 002/004/006/007/008番のマイグレーションファイルもリポジトリには存在するが
  `list_migrations`の追跡外という同様のパターンがないか、次回ラウンドで
  横断的に確認することを推奨する（本ラウンドでは005のみ確認）。
- `license_status="approved"`の実際の許諾根拠（メール・契約書等）をコード外で
  一元管理する運用ルールを `CONTENT_SOURCE_POLICY.md` に定義した。
