# 独自データレポート — 将来計画

作成日: 2026-07-12
ステータス: 計画のみ。**実データに基づくレポートは今回一切公開しない。**

## 0. 前提

AEO/SEOの観点で「Loop Vocabularyだけが持っているデータに基づく一次情報」は、他サイトに
転載・引用されやすく（被リンク獲得）、AI検索エンジンの引用元にもなりやすい強力なコンテンツ
資産になり得る。ただし、現時点ではユーザー数・行動データの蓄積量が少なく、実データを
公開すると以下のいずれかの問題が起きる。

- サンプルサイズが小さすぎて統計的に無意味な数字を「事実」として発信してしまう
- 特定の個人の行動が特定できてしまう（例:「この単語帳の共有数1件」＝誰が使ったか分かる）
- 後で誤りが発覚した場合、サイト全体の信頼性を損なう

そのため、このドキュメントは「どんなレポートを将来作れるか」「そのために今から何を
記録しておくべきか」の設計にとどめ、**実データの計算・公開はサンプルサイズと期間の
最低基準を満たすまで行わない。**

---

## 1. 将来公開できるレポート案

### 1-1. 忘れられやすい英単語ランキング

**内容**: SRS（間隔反復）で「もう一度」を選ばれた回数が多い単語トップN。
**必要なデータ**: `words.wrong_count` / `words.correct_count` / `words.streak`
（既存カラム、記録済み）。ただし現状は**ユーザーごとの`words`行が独立しており、
同じ単語でもユーザーごとに別レコード**のため、複数ユーザーを横断して「単語ごとの
平均誤答率」を出すには`word`列でのテキスト集約が必要（大文字小文字・表記ゆれの正規化が課題）。
**判断**: 記事「英単語の覚え方」系ガイドとの相性が良く、優先度は高いが、正規化ロジックの
整備が前提。

### 1-2. 英検級別でつまずきやすい単語

**内容**: 英検3級〜1級それぞれで、正答率が低い単語トップ5〜10。
**必要なデータ**: `material_words.level`（既存）と、その単語をインポートしたユーザーの
`words.correct_count`/`wrong_count`の突合。現状は1-1と同じ正規化課題に加え、
「どの教材から来た単語か」を`words`テーブルが保持していないため、**`words`に
`source_material_word_id`のような参照カラムを追加しないと正確な集計ができない**。
**判断**: 英検対策記事群（`/guide/eiken-*`）との相性が非常に良いが、実装コストは中〜高。

### 1-3. 語彙力チェック結果の分布

**内容**: `/vocab-check`（一般・英検・TOEIC）の結果レベル別ユーザー割合。
**必要なデータ**: **現状、`/vocab-check`の結果はDBに一切保存されていない**
（GA4イベントとしてのみ送信され、サーバー側の集計テーブルは存在しない）。
このレポートを作るには、GA4のBigQueryエクスポート機能を使うか、匿名の集計専用
テーブル（個人と紐付かない、結果と大まかな日時のみ）を新設する必要がある。
**判断**: 実装コストが最も低い（GA4のイベントは既にPhase 1で送信開始済み）。
GA4→BigQueryのエクスポートが使えるようになれば、コード変更なしで分析可能。

### 1-4. 辞書検索されやすい単語ランキング

**内容**: `/dictionary`で検索された単語のトップN（＝検索需要の高い英単語）。
**必要なデータ**: 現状、検索クエリ自体はDBに保存されていない
（Phase 1で追加した`trackDictionarySearchExecuted`等はGA4イベントのみで、
検索語そのものはイベントパラメータとして送っていない — 個人情報ではないが、
検索語を大量に送るとGA4のイベント数上限やコスト面の考慮が必要になるため、
現状は意図的に送っていない）。将来このレポートを作る場合は、検索語を軽量な
専用ログテーブル（`ai_usage_events`と同様、service_role経由のみで読み書き）に
記録する設計が現実的。
**判断**: `/dictionary/[word]`ページ増設の優先順位付けに直接使えるため、
プロダクト判断の観点でも価値が高い。

---

## 2. 必要なログ（レポート別まとめ）

| レポート | 現状のログ状況 | 追加で必要なもの |
|---|---|---|
| 忘れられやすい単語 | 部分的にあり（`words`テーブル） | 単語表記の正規化ロジック |
| 英検級別つまずき単語 | 教材側のみあり | `words`→`material_words`の参照カラム |
| 語彙力チェック結果分布 | GA4イベントのみ | GA4→BigQueryエクスポート、またはDB集計テーブル新設 |
| 辞書検索ランキング | 無し | 検索語ログテーブル新設（service_role限定） |

---

## 3. 匿名化方針

- 個人（ユーザーID・メールアドレス・氏名）と紐付く形では**一切公開しない**
- レポートに載せるのは「集計後の数値・単語・割合」のみ。個々のユーザーの回答内容や
  単語帳の中身を特定できる形では出さない
- 集計元データがどれだけ細かくても、公開する最小単位は「単語1件あたりの集計値」まで
  （「ユーザーAさんの正答率」のような個人単位の値は公開しない）
- IPアドレス・デバイス情報等、個人特定につながりうる付随情報は集計軸に使わない

---

## 4. 公開してよい集計粒度

- OK: 「"alleviate" は英検準1級学習者の中で正答率が最も低い単語の1つ」
- OK: 「語彙力チェック受験者の32%が『大学受験レベル』と判定された」（サンプルサイズ明記の上で）
- NG: 「◯◯高校の生徒はこの単語が苦手」（属性を絞り込みすぎて特定個人・特定集団が推測できる粒度）
- NG: 単語帳タイトル・説明文など、ユーザーの自由入力テキストをそのまま集計対象にすること
  （文章になっていると個人が書いた文言だと分かってしまうため、選択肢的な集計値のみ扱う）

---

## 5. 個人情報を出さないルール

- レポート作成用のクエリは、氏名・メールアドレス・IPアドレス等の個人情報カラムを
  **selectしない**（集計に使う必要がある場合も、集計後は破棄しJOINキーとしてのみ使う）
- レポート記事の文中に「特定ユーザーの実例」を出さない（架空の例・合成データのみ使う）
- 学習者に未成年が多いことを踏まえ、年齢・学年などの属性軸での公開は行わない
  （このサービスは年齢・学年を収集していないため、そもそも技術的に不可能だが、
  将来収集するようになった場合も同様の方針を維持する）

---

## 6. 最低サンプルサイズ

- 単語単位の集計（例: 正答率）は、**その単語に対する回答が最低100件**貯まるまで
  レポートに含めない
- レベル別・級別の分布（例: 語彙力チェック結果分布）は、**そのカテゴリの合計回答者数が
  最低300件**貯まるまで公開しない
- 上記の基準を満たさない項目は、レポートから静かに除外する（「データ不足」として
  無理に載せない。件数を水増ししたり、少数データを「傾向」として一般化しない）

---

## 7. 捏造・少数データの誤解を避けるルール

- 「〜と言われています」「多くの学習者が」のような、根拠のない一般化表現を使わない
- レポートには必ず「集計期間」「対象母数（n=◯◯）」を明記する
- 速報性より正確性を優先する。月次・四半期など固定間隔で集計し直し、
  「初回公開時の少ないデータのまま更新されない」状態を避ける
- 実データが集まる前にレポートページの「型」だけを先に公開することはしない
  （空のグラフ・仮の数字を見せることは信頼性を損なうため行わない）

---

## 8. 今回のスコープ外であることの確認

このラウンドでは、上記のいずれのレポートも実データでの計算・公開を行わない。
Phase 1で追加したGA4イベントの蓄積が始まったばかりであり、6章の最低サンプルサイズに
遠く及ばないため。次回以降、十分なデータが貯まった時点で本ドキュメントを元に
実装を検討する。
